using HotChocolate.Authorization;
using HotChocolate.Types;
using Microsoft.EntityFrameworkCore;
using Model.Entities;

namespace TestGraphql;

/*
public class Query
{
    public IQueryable<Yeet> GetYeets([Serivce] BloggingContext ctx)
        => ctx.Blogs.Include(b => b.Posts).Where(b => b.IsPublic).OrderBy(b => b.Id);
}
*/